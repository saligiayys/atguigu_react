/* 
	该模块是用于定义action对象中type类型的常量值，目的只有一个：便于管理的同时防止程序员单词写错
*/
export const INCREMENT = 'increment';
export const DECREMENT = 'decrement';
//如果用const定义的是常量，则用大写。（和java有些区别，不是说用const定义的变量都是大写。比如我们之前用const定义的对象，都是小写。）